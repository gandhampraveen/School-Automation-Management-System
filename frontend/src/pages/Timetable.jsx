import React from 'react';

export default function Timetable() {
  return (
    <div className="feature-card">
      <h3>Timetable</h3>
      <p>Render weekly schedules from the timetable API once the backend is wired up.</p>
    </div>
  );
}