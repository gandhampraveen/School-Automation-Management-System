# Frontend

This folder mirrors the split layout requested for the school automation project.

Suggested structure:
- `src/components`
- `src/pages`
- `src/services`
- `src/App.jsx`

The actively running React app still lives in the workspace root `src/` folder, but this folder is now scaffolded so the repository clearly reflects the intended frontend/backend split.